name: Update Nodes

on:
  schedule:
    - cron: '0 * * * *'  # 每小时执行一次
  workflow_dispatch:      # 允许手动触发

jobs:
  update-nodes:
    runs-on: ubuntu-latest
    
    # 🔥🔥🔥 核心修复：必须加上这几行权限设置 🔥🔥🔥
    permissions:
      contents: write    # 允许 Action 写入代码 (git push)
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: pip install requests

      - name: Run update script
        env:
          SHADOW_VIPER_API: ${{ secrets.SHADOW_VIPER_API }}
        run: python update_nodes.py

      - name: Commit and push changes
        run: |
          git config --global user.name 'Shadow Viper Bot'
          git config --global user.email 'bot@shadowviper.com'
          git add public/nodes.json
          # 只有当文件有变化时才提交，防止空提交报错
          git diff --quiet && git diff --staged --quiet || (git commit -m "🔄 Auto update nodes $(date)" && git push)
